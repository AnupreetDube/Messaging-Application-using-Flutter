name: Flutter Messenger
description: A Chatting App developed using Flutter
publish_to: 'none'
version: 1.0.0+1
environment:
  sdk: ">=2.7.0 <3.0.0"
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.3
  splashscreen: ^1.2.0
  firebase_auth: ^0.18.0+1
  cloud_firestore: ^0.14.0+2
  firebase_core: ^0.5.0
  modal_progress_hud: ^0.1.3
  firebase_storage: ^4.0.0
  image_picker: ^0.6.7+7
  geolocator: ^6.1.3
  geocoder: ^0.2.1
dev_dependencies:
  flutter_test:
    sdk: flutter
  uses-material-design: true
  assets:
    - assets/images/
